      <div class="panel panel-primary">
        <!-- Default panel contents -->
        <div class="panel-heading" style="text-align: center; font-weight: bold">EMPLEADOS
        </div>
        <!-- Table -->
        <table id="tablaEmpleados" class="table table-striped table-bordered" cellspacing="0" width="100%">
          <thead>
           <tr>
            {% for campo in cabeceras %}
              <th>{{campo}}</th>
            {% endfor %} 
          </tr>
        </thead>
        <tbody>
          {% for empleado in filas %}
            <tr>
              {% for campo in empleado %}

                <td>
                  {{campo}}
                </td>
              {% endfor %}
            </tr>
          {% endfor %} 
        </tbody>
      </table>
    </div>